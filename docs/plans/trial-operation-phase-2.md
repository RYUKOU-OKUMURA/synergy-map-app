# Phase 2 実事業試験運用チェックリスト

作成日: 2026-05-31

## 目的

開発者本人が実事業データを使い、週次/月次の事業整理で継続利用できる状態を確認する。

Phase 2では新しいDBテーブルを増やさず、既存の`action_items`、`map_notes`、`suggestions`、`versions`、`export_jobs`を使って、「今日見るべきこと」と「次に動くこと」に自然に着地する導線を整える。

## 完成条件

- [x] `pnpm format:check`が通る。
- [x] `cd src-tauri && cargo fmt -- --check`が通る。
- [x] `pnpm lint`が通る。
- [x] `pnpm build`が通る。
- [x] `cd src-tauri && cargo test`が通る。
- [x] `pnpm tauri dev`で起動し、既存プロジェクトを開いたときに「今日」ビューが初期表示される。
- [x] 実事業メモで、入力、AI抽出、マップ生成、次の一手、確認事項、メモ、名前付き保存、Markdown/CSV出力まで通し運用できる。

## 今日ビュー

- [x] プロジェクト内ナビに「今日」が表示される。
- [x] 既存プロジェクト選択時の初期表示が「今日」になる。
- [x] 新規マップ作成時は従来通りマップ作成フローが優先される。
- [x] 情報がない状態では、情報追加が次アクションとして表示される。
- [x] source chunksがあり抽出カードがない状態では、AI抽出が次アクションとして表示される。
- [x] 抽出カードがありマップがない状態では、マップ生成が次アクションとして表示される。
- [x] 追加・削除ソースや未反映カードがある状態では、再抽出/再生成推奨が表示される。
- [x] 未完了確認事項がある状態では、確認事項の整理が次アクションとして表示される。
- [x] 落ち着いた状態では、名前付き保存とMarkdown/CSV出力が次アクションとして表示される。

## 確認事項

- [x] `action_items.status = open`の項目が優先度順に表示される。
- [x] 「今日」ビューから確認事項を完了にできる。
- [x] 「今日」ビューから確認事項を見送りにできる。
- [x] 完了/見送り後、workspaceと表示が即時に更新される。
- [x] 「記録」ビューでは既存通り確認事項を詳細編集できる。

## 次の一手

- [x] 未却下の`suggestions`が「今日」ビューに表示される。
- [x] 施策を確認事項へ変換できる。
- [x] 変換した確認事項は`source_type = "suggestion"`、`source_id = suggestionId`で保存される。
- [x] 同じsuggestion由来の未完了確認事項がある場合、重複作成されない。
- [x] 存在しないsuggestionからの作成はエラーになる。

## 最近の状態

- [x] 最近の思考メモ、会議メモ、日次メモを確認できる。
- [x] 最近の名前付き保存を確認できる。
- [x] 最近のMarkdown/CSV出力履歴を確認できる。
- [x] 必要に応じて「記録」「履歴」「出力」ビューへ移動できる。

## 見せる前チェック

- [ ] 実事業メモから、機密情報、個人情報、見せたくない数値をマスキングする。
- [ ] 施策詳細パネルを開いた後、今日 / 記録 / 出力 / 履歴 / 設定 / 情報ソースへ移動してもパネルが残らないことを確認する。
- [ ] AI生成、Composer / Codex fallback、ローカルドラフトの表示を見せる前に確認する。
- [ ] 出力先設定、Markdown出力、CSV bundle出力、出力先を開く操作を通しで確認する。
- [ ] 長い調査メモを新規作成画面に貼れることを確認する。
- [ ] 複数SNS URLをまとめ貼りして行に分かれることを確認する。
- [ ] URL本文未取得の説明が新規作成時に見えることを確認する。
- [ ] 生成中の段階表示が見えることを確認する。
- [ ] AI抽出ボタンのdisabled理由が読めることを確認する。

デスクトップアプリ確認メモ 2026-06-01:

- `Synergy Map.app`のdebug bundleをComputer Useで操作し、既存マップの「今日」ビュー、施策ビュー、履歴ビュー、出力ビューを確認した。
- 施策詳細パネルは閉じるボタンで閉じられ、詳細パネルを開いたまま「今日」へ移動しても右パネルは残らなかった。記録 / 設定 / 情報ソースは未確認のため、見せる前チェックは未完了のまま残す。
- 履歴ビューで`Composer生成`と`ローカルドラフト`表示を確認した。Codex fallback表示は未確認のため、見せる前チェックは未完了のまま残す。
- Markdown出力、CSV bundle出力、CSV出力先をFinderで開く操作は確認した。出力先設定の変更操作は未確認のため、見せる前チェックは未完了のまま残す。

## 実事業シナリオ

- [x] 直近の事業メモを投入し、抽出カードの要確認項目を整理できる。
- [x] 売上マップを見て、詰まりまたは未接続の可能性を1つ以上説明できる。
- [x] 次に試す一手を確認事項へ変換し、今日の確認対象として扱える。
- [ ] 思考メモを追加し、再起動後も「今日」ビューから最近の状態を確認できる。
- [x] Markdown/CSV出力が月次振り返りや相談準備に使える内容になっている。

## 実事業QAメモ

実施日: 2026-05-31
対象メモ: `PBL 実事業QA 2026-05-31`

### 判定

- Phase 2完了条件: 主要な実事業運用フローは満たす。最近の状態カードの個別視認は未実施として残す。
- 週次/月次で使うイメージ: 持てる。特にLINE配信、ブログ、予約、再来院、商品導線を月次で並べる用途に合う。

### 分かったこと

- AI送信前確認では、初回入力、業種、事業メモ、商品情報、登録済み情報ソース数が読めた。
- 抽出カードは19件生成され、事業、商品・サービス、集客チャネル、顧客接点、財務/課題、データソースが分かれた。
- 売上マップでは、整体施術、再来院フォロー、LINE配信、健康ブログ、予約管理、AI相談、商品案内のつながりと分断が読めた。
- 次の一手は、LINE配信から再来院予約へ直結させる施策など、週次運用に落とし込める具体度だった。
- Markdown/CSVは、概要、導線、AIコメント、次の一手、確認事項、思考メモを含み、月次振り返りや相談準備に使える。
- 再起動後、既存プロジェクトを開くと「今日」ビューが初期表示され、未完了確認事項と施策候補から再開できた。
- 思考メモ、名前付き保存、Markdown/CSV出力はDBと出力ファイル上で確認できた。今日ビュー下部の最近の状態カードは個別視認できていないため未実施のまま残す。

### 摩擦メモ

- 迷った画面: 施策カードの詳細パネルを開いたまま他ビューへ移動すると、記録ビューや今日ビューの操作領域を邪魔した。
- ボタン名・導線の違和感: 施策詳細を開いた後、明示的な閉じるボタンが見えにくかった。
- 対応メモ: 詳細パネルに閉じるボタンを追加し、今日 / 記録 / 出力 / 履歴 / 設定 / 情報ソースへ移動したときは選択状態をクリアする方針にした。
- AI出力が抽象的だった箇所: なし。実務に使うには、次回以降に予約数、再来院数、LINEクリック数を足すとさらに良い。
- 今日ビューで次に何をすればよいか分かりにくい状態: なし。未完了確認事項がある状態では整理が次アクションとして出た。
- Markdown/CSV出力で足りない項目: 月次KPIの実数カラムは今後ほしいが、Phase 2の振り返り用途としては足りている。
- 実務上、毎週または毎月使う気になるか: 使える。週次は確認事項の消化、月次はMarkdown/CSV出力で振り返る流れが自然。

## Phase 2対象外

- 差分比較
- 本格チャットUI
- PDF強化、OCR、URL本文自動取得
- レイヤー型深掘りマップ
- Windows通し確認、配布、署名、notarization
